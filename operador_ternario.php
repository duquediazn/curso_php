<?php
    /*
    operacion ? valor1 : valor2;
    variable = (operacion) ? valor1 : valor2;

    //Operador ternario reducido:
    $variable ?: null
    //?: es un operador ternario reducido (Elvis operator, ternary shorthand operator or null coalescing shorthand) que evalúa valores truthy o falsy. Equivale a:
    $variable ? $variable : null;

    //Operador de fusión de nulo (Null coalescing operator):
    $variable ?? null;
    //?? evalúa si algo está definido y no es null. Si está definido y no es null $variable -> $variable, si no está definida o es null $variable -> null
    */