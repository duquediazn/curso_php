<?php
    /*
    operacion ? valor1 : valor2;
    $variable = (operacion) ? $valor1 : $valor2;

    //Operador ternario reducido:
    $variable ?: otroValor;
    // ?: Operador ternario reducido o simplificado (Operador de coalescencia a la izquierda, Elvis operator, ternary shorthand operator...)  
    // evalúa valores truthy o falsy. Equivale a:
    $variable ? $variable : otroValor;

    //Operador de fusión de nulo (Null coalescing operator):
    $variable ?? null;
    // ?? evalúa si algo está definido y no es null. Si está definido y no es null $variable -> $variable, 
    // si no está definida o es null $variable -> null
    */
    